if vim.g.srcerite_inverse_visual == nil then
   vim.g.srcerite_inverse_visual = true
end

if vim.g.srcerite_inverse_search == nil then
   vim.g.srcerite_inverse_search = false
end

if vim.g.srcerite_inverse_match_paren == nil then
   vim.g.srcerite_inverse_match_paren = false
end
